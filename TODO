opimd-notes:
 * filtering tags with anchors (py-elm!)
 * editing tags (opimd!)
 * searching
 * async quering
opimd-contacts:
 * editing contacts
 * adding new contacts
 * contact history
opimd-notifier:
 * change Reply to Actions
 * add Actions into missed call
opimd_reply:
 * add contact selector
 * support sending CSM
 * support unicode
opimd-messages:
 * rewrite!
